# Как работать в VSCode

`Ctrl+F5` (Debug: Start Without Debugging) -- запуск без отладки
`F5` (Debug: Start Debugging) -- запуск с отладкой
`Ctrl+Shift+B` (workbench.action.tasks.build) -- сборка всего проекта
