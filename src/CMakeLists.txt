add_subdirectory(core)
add_subdirectory(framework)
add_subdirectory(render)
add_subdirectory(io)
if(OpenCASCADE_FOUND)
  add_subdirectory(numgeom)
endif()
if(UNIX)
  add_subdirectory(xdg-shell-client)
endif()
